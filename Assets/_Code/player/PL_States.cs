namespace Player {
    public enum PlayerState {
        walking,
        sliding,
        slamming,
        crawling,
        dead
    }

    public enum MovementState {
        start,
        middle,
        end,
        none
    }

    

    public enum AdminState {
        standard,
        noclip
    }
}
